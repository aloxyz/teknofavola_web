import type { SocialFormat } from '$lib/types/directus';

// Display names for each format's social group — proper nouns, identical in both languages.
export const SOCIAL_FORMAT_LABEL: Record<SocialFormat, string> = {
	teknofavola: 'TEKNOFAVOLA',
	once_upon_a_time: 'ONCE UPON A TIME',
	fable_label: 'THE FABLE LABEL',
	fable_studio: 'THE FABLE STUDIO'
};
